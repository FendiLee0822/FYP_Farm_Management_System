@model inventory_User_Model

@{
    ViewData["Title"] = "Inventory Update Page";
}

<div class="col-md-4">
    <div class="mb-3">
        <h2 class="text-primary">Inventory Update Page</h2>
        <hr />
    </div>

    <form asp-action="inventory_Update_Page_Employee" method="post">
        <input type="hidden" asp-for="Inventory.InventoryId" />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group">
            <input type="hidden" asp-for="Inventory.InventoryId" />
        </div>
        <div class="form-group">
            <label asp-for="Inventory.InventoryName" class="control-label">Inventory Name</label>
            <input asp-for="Inventory.InventoryName" class="form-control" readonly />
            <span asp-validation-for="Inventory.InventoryName" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="Inventory.InventoryAmount" class="control-label">Inventory Amount</label>
            <input asp-for="Inventory.InventoryAmount" class="form-control" required/>
            <span asp-validation-for="Inventory.InventoryAmount" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="Inventory.UserUpdate"></label>
            <select asp-for="Inventory.UserUpdate"
                    asp-items="@(new SelectList(Model.Users, "UserId", "UserId"))"
                    class="form-control">
            </select>
        </div>
        <div class="form-group">
            <input type="hidden" asp-for="Inventory.InventoryUpdateTime" />
        </div>
        <button type="submit" class="btn btn-primary" onclick="return confirm('Are you sure you want to update inventory?')">Save</button>
        <a asp-controller="Inventory" asp-action="inventory_Information_Page_Employee" class="btn btn-primary">Back To Inventory List</a>
    </form>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
